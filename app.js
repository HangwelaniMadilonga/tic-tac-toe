function createPlayer (name) {
    const Player = name;
    const playerSymbol = "";
    let playerScore = 0;
    const getPoint = () => point;
    const givePoint = () => point++;
  
    return { };
  }